<% require 'uri' -%>

<% @backends.keys.sort.each do |name| -%>

<% if @backends[name].kind_of?(Array) -%>

director <%= name %> round-robin {
<% @backends[name].sort.each do |member| -%>
  <% uri = URI(member) -%>
  {
     .backend = {
       .host = "<%= uri.host %>";
       .port = "<%= uri.scheme %>";
       .probe = {
         .url = "<%= uri.path %>";
       }
     }
  }
<% end -%>
}

<% else -%>

backend <%= name %> {
    .host = "<%= @backends[name] %>";
}

<% end -%>

<% end -%>

backend status {
   .host = "localhost";
   .port = "8080";
   .connect_timeout = 10s;
   .first_byte_timeout = 10s;
   .between_bytes_timeout = 10s;
}

sub vcl_recv {
  set req.grace = 10h;
  if (req.http.host == "localhost") {
    set req.backend = status;
  }

<% @vhost.keys.sort.each do |name| -%>
  if (req.http.host == "<%= name %>.<%= @domain %>" || req.http.host == "<%= @vhost[name] %>") {
    set req.http.host = "<%= @vhost[name] %>";
    set req.backend = <%= name %>;
  }
<% end -%>
}

sub vcl_fetch {
  set beresp.grace = 10h;
}
